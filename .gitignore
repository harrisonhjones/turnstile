# Compiled binary
/turnstile

# SQLite database and its WAL/SHM sidecars
*.db
*.db-wal
*.db-shm

# Local environment
.env

# air hot-reload artifacts
/tmp/
build-errors.log

# Go build cache dir (if any)
/build/

# GoReleaser output (also the source the prebuilt Docker build COPYs from in CI)
/dist/
